#include "holberton.h"

/**
 * _isupper : Function to find uppercase characters
 * @c: is a character to test
 * Return: 1 if is uppercase, 0 for anyelse
 */

int _isupper(int c)
{

if (c >= 'A'  && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
